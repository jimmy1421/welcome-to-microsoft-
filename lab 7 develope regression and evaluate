import pandas as pd
import numpy as np
from sklearn import linear_model
import matplotlib.pyplot as plt
#upload file from local drive
from google.colab import files
# The file will be read directly from the provided path, so files.upload() is not needed here.
import io
df = pd.read_csv('/content/temperatures.csv')
df
%matplotlib inline
plt.xlabel('YEAR')
plt.ylabel('JAN')
plt.scatter(df.YEAR, df.JAN, color='red', marker='*')
new_df=df
new_df.drop(df.iloc[:, 2:18], inplace = True, axis = 1)
new df
new_df=new_df.drop('JAN', axis='columns')
new_df
JanTemp = df.JAN
JanTemp
print(new_df)
# Create linear regression object
reg = linear_model.LinearRegression()
reg.fit(new_df, JanTemp
from sklearn.model_selection import train_test_split
X_train,X_test,y_train,y_test = train_test_split(new_df, JanTemp, test_size=0.25, random_state=2)
from sklearn.linear_model import LinearRegression
1r = LinearRegression()
lr.fit(X_train,y_train)
y_pred = 1r.predict(X_test)
y_pred
from sklearn.metrics import mean absolute error
print("MAE",mean_absolute_error(y_test,y_pred))
from sklearn.metrics import mean_squared_error
print("MSE", mean_squared_error(y_test,y_pred))
